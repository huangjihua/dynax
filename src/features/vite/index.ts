import { createOrOverwriteFile, createOrUpdateJsonConfigFile } from '../../utils/file'
import { GenericObject, FeatureType } from "../../types";
import addEnv from '../env';


/**
 * 初始化 Vite
 *
 * @param targetDir 目标目录
 * @param features 功能列表
 * @returns 无返回值
 */
export default function initVite(targetDir: string, features: string[]) {

}